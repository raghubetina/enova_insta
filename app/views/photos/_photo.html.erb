<div class="row">
  <div class="col-md-2 col-md-offset-1">
    <div class="media">
      <%= link_to image_tag(photo.user.avatar_url, :class => "media-object"), photo.user %>
      <div class="media-body">
        <h4 class="media-heading"><%= photo.user.nickname %></h4>
        <%= time_ago_in_words photo.created_at %> ago
      </div>
    </div>
  </div>
  <div class="col-md-7 well">
    <div class="thumbnail">
      <%= image_tag photo.image, :class => "img-responsive" %>
      <div class="caption">
        <%= content_tag "p", photo.caption, :class => "lead"  %>
        <p>
          <%= link_to "<i class='fa fa-search-plus'></i>".html_safe, photo, :class => "btn btn-primary" %>

          <%# if current_user == photo.user %>
            <%= link_to "<i class='fa fa-edit'></i>".html_safe,
                        edit_photo_path(photo),
                        :class => "btn btn-warning" %>
            <%= link_to "<i class='fa fa-trash-o'></i>".html_safe,
                        photo,
                        :method => "delete",
                        :class => "btn btn-danger",
                        :remote => true %>
          <%# end %>

          <% if photo.fans.include?(current_user) %>
            <%= link_to "<i class='fa fa-heart'></i>".html_safe,
                        Favorite.find_by(:user_id => current_user.id, :photo_id => photo.id),
                        :method => "delete",
                        :class => "btn btn-danger" %>
          <% else %>
            <%= link_to "<i class='fa fa-heart-o'></i>".html_safe,
                        favorites_url(:favorite => { :photo_id => photo.id }),
                        :method => "post",
                        :class => "btn btn-success" %>
          <% end %>
        </p>
      </div>
    </div>
    <ul class="media-list">
      <% photo.comments.each do |comment| %>
        <%= render 'comments/comment', :comment => comment %>
      <% end %>

      <%= render 'comments/inline_form', :comment => Comment.new(:photo_id => photo.id) %>
    </ul>
  </div>
</div>




