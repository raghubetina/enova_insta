<li class="media">
  <%= link_to image_tag(comment.user.avatar_url, :class => "media-object"), comment.user, :class => "pull-left" %>
  <div class="media-body">
    <p class="lead"><%= comment.content %></p>
  </div>
</li>
